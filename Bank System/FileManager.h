#pragma once
#include "DataSourceInterface.h"
class FileManager :
    public DataSourceInterface
{

    //implementation of every method in DataSourceInterface

   /* addClient should save client info in clients.txt.
        addEmployee should save employee info in employees.txt.
        addAdmin should save employee info in admins.txt.
        getAllClients(), getAllEmployees(), and getAllAdmins() should retrieve data from files.
        removeAllClients(), removeAllEmployees(), and removeAllAdmins() should remove all data from the files.*/

};

